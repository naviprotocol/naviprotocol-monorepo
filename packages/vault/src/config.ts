/** Base URL of the NAVI open API used to discover vaults, positions, and pending requests. */
export const OPEN_API_URL = 'https://open-api.naviprotocol.io/api'

/** Max value of an on-chain `u64`. Used as the "withdraw everything" sentinel amount. */
export const U64_MAX = 18_446_744_073_709_551_615n
